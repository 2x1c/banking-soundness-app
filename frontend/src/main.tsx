import React from 'react';
import ReactDOM from 'react-dom/client';
import BankingSoundnessApp from './banking_soundness_app';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BankingSoundnessApp />
  </React.StrictMode>
);